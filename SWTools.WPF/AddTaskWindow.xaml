<Window x:Class="SWTools.WPF.AddTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SWTools.WPF"
        mc:Ignorable="d"
        Title="添加下载任务"
        Height="600" Width="900"
        MinHeight="400" MinWidth="800"
        Background="#f3f3f3"
        WindowStartupLocation="CenterOwner"
        RenderOptions.BitmapScalingMode="HighQuality">
    <DockPanel>
        <!--底栏-->
        <DockPanel DockPanel.Dock="Bottom" LastChildFill="False" Background="#e4e4e4">
            <Button x:Name="BtnOk" Margin="10 10 20 10" VerticalAlignment="Top" Height="40" DockPanel.Dock="Right"
                    Style="{DynamicResource AccentButtonStyle}" Click="BtnOk_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE8FB;" VerticalAlignment="Center"/>
                    <TextBlock Text=" 确定"/>
                </StackPanel>
            </Button>
            <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center" Margin="20 10 10 10"
                       Text="提示：输入要下载的 ID → 解析信息 → 补充缺失的信息（如有）→ 完成"/>
        </DockPanel>
        <Separator DockPanel.Dock="Bottom" BorderBrush="#d4d4d4"/>

        <DockPanel>
            <!--物品ID输入-->
            <DockPanel Margin="20 20 10 20" Width="347" DockPanel.Dock="Left">
                <TextBlock Text="在下面输入要下载的物品 ID（一行一个 ID）" DockPanel.Dock="Top"/>
                <DockPanel LastChildFill="False" DockPanel.Dock="Bottom">
                    <Button x:Name="BtnParse"  VerticalAlignment="Top" Height="40" DockPanel.Dock="Right" Style="{DynamicResource AccentButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE721;" VerticalAlignment="Center"/>
                            <TextBlock Text=" 解析信息"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnHelpItem"  VerticalAlignment="Top" Height="40" DockPanel.Dock="Left">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE897;" VerticalAlignment="Center"/>
                            <TextBlock Text=" 物品 ID 是什么"/>
                        </StackPanel>
                    </Button>
                </DockPanel>
                <TextBox TextWrapping="Wrap" AcceptsReturn="True" Margin="0 10 0 10"/>
            </DockPanel>
            <Separator DockPanel.Dock="Left" BorderBrush="#d4d4d4" Margin="0 20 0 20"/>
            <!--解析状态-->
            <DockPanel Margin="10 20 20 20">
                <TextBlock Text="解析状态" DockPanel.Dock="Top"/>
                <DockPanel DockPanel.Dock="Bottom">
                    <TextBlock Text="App ID：" VerticalAlignment="Center"/>
                    <Button x:Name="BtnEdit"  VerticalAlignment="Top" Height="40" DockPanel.Dock="Right" Margin="5 0 0 0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE70F;" VerticalAlignment="Center"/>
                            <TextBlock Text=" 修改信息"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnHelpApp"  VerticalAlignment="Top" Height="40" DockPanel.Dock="Right" Margin="5 0 5 0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE897;" VerticalAlignment="Center"/>
                            <TextBlock Text=" App ID 是什么"/>
                        </StackPanel>
                    </Button>
                    <TextBox Height="35" VerticalAlignment="Center" Margin="5 0 5 0" DockPanel.Dock="Left"/>
                </DockPanel>
                <ListView>
                    
                </ListView>
            </DockPanel>
        </DockPanel>
    </DockPanel>
</Window>
